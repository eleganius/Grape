<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.app.dao.LikeDao">

	<select id="countLike" resultType="long">
		SELECT
		COUNT(*)
		FROM likes
		WHERE article_id = #{articleId}
	</select>
	
	<insert id="insert" parameterType="com.example.app.domain.Like">
		INSERT INTO likes
		(user_id, article_id, created_at)
		VALUES
		(#{loginStatusId}, #{articleId}, NOW())
	</insert>
	
	<delete id="delete" parameterType="int">
		DELETE FROM likes
		WHERE user_id = #{loginStatusId}
		AND article_id = #{articleId}
	</delete>

</mapper>